<div class="row" ng-controller="mainCtrl" ng-init="init()">
  <div class="col-xs-10 col-xs-offset-1">
    <div class="row">
      <form class="form-horizontal">
        <div class="form-group">
          <select class="form-control font-bold" 
            ng-model="categoryId"
            ng-required="true"
            ng-options="option.id as option.summary for option in categoryList">
          </select>
        </div>
        <div class="form-group">
          <input class="form-control" ng-model="summary" placeholder="제목">
        </div>
        <div class="form-group">
          <textarea class="form-control" ng-model="description" rows="3" placeholder="내용"></textarea>
        </div>
        <div class="form-group">
           <button type="submit" ng-click="process()" class="btn btn-primary btn-lg btn-block">{{buttonText}}</button>
        </div>
      </form>
    </div>
  </div>
</div>